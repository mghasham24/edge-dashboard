{
  "name": "payout-bot",
  "version": "1.0.0",
  "description": "Playwright payout bot for RaxEdge — runs headed on Mac, looks like a human",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "install-browser": "npx playwright install chromium"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "playwright": "^1.46.0"
  }
}
